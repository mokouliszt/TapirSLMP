# Generate independent random values of at least 32 URL-safe characters.
SEALER_TOKEN=
LIBERATOR_TOKEN=

# PostgreSQL compose only
POSTGRES_PASSWORD=

# Writes and remote-control commands remain disabled unless explicitly enabled.
ALLOW_STATE_CHANGING_COMMANDS=false
